import { FiltroRutPipe } from './filtro-rut.pipe';

describe('FiltroRutPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroRutPipe();
    expect(pipe).toBeTruthy();
  });
});
